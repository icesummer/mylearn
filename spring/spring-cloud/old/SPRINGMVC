http报文头部：
telen
一、spring security认证方式：
http Basic：用户信息进行base64编码放在http报文头部，安全性问题
Digest认证：
1、浏览器对用户信息，http请求方法，uri，进行md5计算，将摘要信息发送给服务器；
2、服务器web容器解析报文头部解析到username，从数据库获取密码，同样将用户信息，http请求方法，uri等进行md5编码；
3、对比客户端和服务端的编码摘要返回登录结果；
报文可以被复制使用

4、X.509证书认证 ()

5、LDAP认证(轻量级)：统一认证
6、Form表单认证

二、(认证处理处理器，安全角色处理器)
